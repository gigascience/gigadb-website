version: '2'
services:
  pg9_3:
    image: postgres:9.3
    volumes:
      - ./sql/bootstrap_gigadb.sql:/docker-entrypoint-initdb.d/gigadb.sql
      - ./sql:/var/sql
    ports:
      - '5432:5432'

  updater:
    image: files-url-updater
    volumes:
      - ./sql:/var/sql
      - .:/app
