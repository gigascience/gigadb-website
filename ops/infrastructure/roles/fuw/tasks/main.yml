
- name: Create a DB user
  command: psql -U postgres -c "CREATE USER {{ fuwdb_user }} WITH PASSWORD '{{ fuwdb_password }}';"
- name: Create the database
  command: psql -U postgres -c "CREATE DATABASE {{ fuwdb_database }} WITH OWNER {{ fuwdb_user }};"
