defaults
auth           on
tls            on
tls_trust_file /etc/ssl/certs/ca-certificates.crt
logfile        ~/.msmtp.log
account        gmail
host           smtp.gmail.com
port           587
from           ${SERVER_EMAIL}
user           ${SERVER_EMAIL}
password       ${SERVER_EMAIL_PASSWORD}
account default : gmail
