#!/bin/sh

# Generated by chef. Don't ****ing touch!
# - unless you're editing the template (set_env.sh.erb) -

<% db = node[:gigadb][:db] -%>
export DB_HOST=<%= db[:host] %>
export DB_NAME=<%= db[:database] %>
export DB_USER=<%= db[:user] %>
export DB_PASS=<%= db[:password] %>

export PGDATABASE=$DB_NAME
export PGHOST=$DB_HOST
export PGUSER=$DB_USER
export PGPASSWORD=$DB_PASS

# activate virtualenv
. <%= node[:gigadb][:python][:virtualenv] %>/bin/activate
