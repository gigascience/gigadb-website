# Allow all outgoing connections as default
-P OUTPUT ACCEPT
# Block everything else coming into the server
-P FWR DROP
# Block everything else that is being forwarded as we are not a router
-P FORWARD DROP